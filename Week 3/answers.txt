sort1 uses: Bubble Sort

How do you know?: It was the fastest on already sorted inputs, because of detecting no swaps in a single pass and exiting early.

sort2 uses: Merge Sort

How do you know?: It was the fastest algorithm on large randomized inputs, since merge sort has a better time complexity than the other two algorithms.

sort3 uses: Selection Sort

How do you know?: Elimination method.